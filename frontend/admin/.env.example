# URL du backend Express (sans slash final). Copier vers .env puis npm run dev (le dépôt ignore .env).
# Étape 2 : liste transactions. Étape 3 : stats + graphique dérivés de cette API.
VITE_API_BASE_URL=http://localhost:3000
