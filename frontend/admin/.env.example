# URL de l’app portail client (Vite, port 5174 par défaut). Lien « Client » dans l’admin.
VITE_CLIENT_APP_URL=http://localhost:5174

# Backend base URL (no trailing slash). If unset, network calls are skipped; console logs still run.
VITE_API_BASE_URL=http://localhost:8000
