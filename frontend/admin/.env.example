# Dev uniquement : URL du backend local (sans slash final). Copier vers .env.
# En production (Vercel), le build ignore cette variable : les appels vont sur /api/… (même origine),
# puis vercel.json proxifie vers Render — pas de CORS.
VITE_API_BASE_URL=http://localhost:3002
